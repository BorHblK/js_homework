
const ageInput = prompt("Введіть кількість років:");

if (ageInput != null && isNaN(ageInput) && ageInput >= 0)
    {
        console.log("ви вели" + ageInput)

        

    }
else {
    console.log("Введіть число яке більше або рівно 0")
}

